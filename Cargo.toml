[package]
name = "compressor"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
base64 = "0.22"
dotenvy = "0.15"
libdeflater = "1.25"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde_json = "1"
